{
  "name": "claude-leaderboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create claude-leaderboard-db",
    "db:migrate": "wrangler d1 execute claude-leaderboard-db --file=migrations/0001_initial.sql && wrangler d1 execute claude-leaderboard-db --file=migrations/0003_add_source.sql && wrangler d1 execute claude-leaderboard-db --file=migrations/0004_drop_old_index.sql && wrangler d1 execute claude-leaderboard-db --file=migrations/0005_add_sharing.sql && wrangler d1 execute claude-leaderboard-db --file=migrations/0006_add_fav_tools.sql && wrangler d1 execute claude-leaderboard-db --file=migrations/0007_add_git_metadata.sql && wrangler d1 execute claude-leaderboard-db --file=migrations/0008_add_git_machine.sql",
    "db:migrate:local": "wrangler d1 execute claude-leaderboard-db --local --file=migrations/0001_initial.sql && wrangler d1 execute claude-leaderboard-db --local --file=migrations/0003_add_source.sql && wrangler d1 execute claude-leaderboard-db --local --file=migrations/0004_drop_old_index.sql && wrangler d1 execute claude-leaderboard-db --local --file=migrations/0005_add_sharing.sql && wrangler d1 execute claude-leaderboard-db --local --file=migrations/0006_add_fav_tools.sql && wrangler d1 execute claude-leaderboard-db --local --file=migrations/0007_add_git_metadata.sql && wrangler d1 execute claude-leaderboard-db --local --file=migrations/0008_add_git_machine.sql",
    "db:seed": "wrangler d1 execute claude-leaderboard-db --file=migrations/0002_seed_invites.sql",
    "db:seed:local": "wrangler d1 execute claude-leaderboard-db --local --file=migrations/0002_seed_invites.sql",
    "git:upload": "node scripts/git-metadata.js"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "satori": "^0.19.2",
    "workers-og": "^0.0.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.65.0"
  }
}
